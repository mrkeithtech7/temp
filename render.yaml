# render.yaml - Minimal Configuration
services:
  - type: web
    name: moon-xmd
    env: node
    region: singapore
    buildCommand: npm install --legacy-peer-deps
    startCommand: npm start
    envVars:
      - key: SESSION_ID
        value: ""
      - key: PREFIX
        value: "."
      - key: OWNER_NUMBER
        value: "263776509966"